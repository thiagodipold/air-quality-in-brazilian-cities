import { createTheme } from "@material-ui/core";

export const theme = createTheme({
  typography: {
    fontFamily: "Ubuntu",
    body2: {
      fontSize: "17px",
      fontWeight: 400,
    },
  },
  palette: {
    info: {
      main: "#fff",
    },
  },
});
